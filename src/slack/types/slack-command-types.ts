import { ParsedUrlQuery } from "querystring";

export interface SlackCommandTypes extends ParsedUrlQuery {
  token: string;
  team_id: string;
  team_domain: string;
  channel_id: string;
  channel_name: string;
  user_id: string;
  user_name: string;
  command: string;
  text: string;
  response_url: string;
}
